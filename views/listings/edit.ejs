<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WanderLust</title>
</head>
<body>
    <h3>Edit your Listing</h3>
    <form method="POST" action="/listings/<%= listing._id %>?_method=PUT">
        <!-- use method to override post request to put request -->
        <div class="mb-3">
            <label for="title" class="form-lable">Title</label> 
            <input name="listing[title]" value="<%=listing.title%>" type="text" class="form-control">
        </div>

        <div class="mb-3">
            <label for="title" class="form-lable">Title</label> 
            <textarea name=listing[description]><%=listing.description %></textarea>
        </div> 
        
        <div class="mb-3">
            <label for="image" class="form-lable">Image Link</label> 
            <input name=listing[image] value="<%=listing.image.url %>" type="text" class="flow-control" >
        </div> 
        
        <div class="mb-3">
            <label for="price" class="form-lable">Price</label> 
            <input name=listing[price] value="<%=listing.price%>" type="number" class="flow-control">
        </div> 
        
        <div class="mb-3">
            <label for="country" class="form-lable">Country</label> 
            <input name=listing[country] value="<%=listing.country%>" type="text" class="flow-control">
        </div> 
        
        <div class="mb-3">
            <label for="location" class="form-lable">Location</label> 
            <input name=listing[location] value="<%=listing.location%>" type="text" class="flow-control">
        </div>            
        <button>Edit</button>
    </form> 
    
</body>
</html>